{
  "name": "barsandlines-site",
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "esbuild": "^0.25.0",
    "tailwindcss": "^3.4.19"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R static/* dist && npx eleventy && npx esbuild src/scripts/script.js --minify --outfile=dist/script.js && npx tailwindcss -c .tailwindcss.js -i src/scripts/styles.css -o dist/styles.css --minify"
  }
}
